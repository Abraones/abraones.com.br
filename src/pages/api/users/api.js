const { Axios } = require("axios");


async function publicCall(path, data){
    try{
        const result = await Axios({
            
        })
    }
    catch(err){
        console.log(err)
    }

}